import { TABLE_NAMES } from '@/app/api/types/database'
import { getInitTableData } from '@/app/api/utils/serverUtils'

// export async function generateStaticParams() {
//   const jobs: { id: string }[] = await getInitTableData(TABLE_NAMES.JOBS)
 
//   return jobs.map(({ id }) => ({
//     slug: id,
//   }))
// }
